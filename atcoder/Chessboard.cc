#include <bits/stdc++.h>
using namespace std;

int main(){
  char chess[8][8];
  for(int beside = 0; beside < 8; beside++){
    for(int vertical = 0; vertical < 8; vertical++){
      cin >> chess[]
    }
  }
}
