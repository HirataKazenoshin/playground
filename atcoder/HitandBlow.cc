#include<iostream>
using namespace std;

int main(void)
{
    int length;
    cin >> length;
    char a[length], b[length];
    for(int i = 0;i < length; i++){
        cin >> a[i];
    }
    for(int i = 0;i < length; i++){
        cin >> b[i];
    }
    int includeonly, includesame
}